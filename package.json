{
  "name": "tf20-website-portal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Task Force 20 public website and protected personnel portal backend scaffold.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node server.js",
    "check": "node --check server.js && node --check cpanel-server.cjs && node --check src/server/app.js && node --check src/server/config.js && node --check src/server/db.js && node --check src/server/middleware/auth.js && node --check src/server/routes/api.js && node --check src/server/routes/pages.js && node --check src/server/services/users.js && node --check src/server/services/portal-data.js && node --check src/server/services/session-store.js && node --check prisma/seed.mjs",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy",
    "prisma:dev": "prisma migrate dev",
    "db:seed": "node prisma/seed.mjs"
  },
  "engines": {
    "node": ">=18.20.0"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "express": "^4.18.3",
    "express-session": "^1.18.0",
    "helmet": "^7.1.0",
    "passport": "^0.7.0",
    "passport-discord": "^0.1.4"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  },
  "prisma": {
    "seed": "node prisma/seed.mjs"
  }
}
